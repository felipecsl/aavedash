# AGENTS Instructions

- Run typechecks, lint, and Prettier before finishing changes (`format`, `typecheck`, `lint` scripts)
- Make sure the project readme is up to date to include main features, usage instructions, caveats, limitations, known bugs, etc.
